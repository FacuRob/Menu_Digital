# Netlify — Frontend admin (Vite)
# En Netlify: "Base directory" = frontend-admin
[build]
  command = "npm run build"
  publish = "dist"

# SPA: cualquier ruta cae en index.html (evita 404 al recargar /admin/*, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
